# Remove Reset Default View Plan

1. Remove the article list context reset button from the reader page while keeping the context summary text.
2. Delete the now-unused reset handler to avoid dead UI logic.
3. Clean up the unused button-specific CSS so the context bar layout stays simple.
4. Run a frontend build to verify the UI removal does not break compilation.
