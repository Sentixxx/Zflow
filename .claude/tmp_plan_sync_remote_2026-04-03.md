## 计划书：2026-04-03 远端同步与冲突处理

1. 先获取远端最新提交，确认本地与 `origin/main` 的分叉状态。
2. 由于当前工作区存在大量未提交修改，先用 stash 暂存全部已跟踪与未跟踪内容，避免 pull/rebase 直接失败。
3. 将本地 `main` 快进或 rebase 到 `origin/main`。
4. 再恢复 stash，把本地修改重新叠到最新远端之上。
5. 若 stash pop 产生冲突，逐文件保留“远端新增能力 + 本地未提交修改”，完成后向用户汇报冲突文件与处理结果。
