## 计划书：2026-04-03 冲突文档重新合并

1. 检查 stash 中本地版本的 `CHANGE / change / task` 内容，并识别本地新增条目。
2. 以当前远端版本为基底，读取各文件已有最大编号。
3. 将本地条目按原语义并入文档末尾，重新编号为“当前最大编号 + 1”起，避免覆盖远端编号。
4. 保持 phase 索引、phase change、phase task 三份文档互相可追溯。
5. 完成后重新检查 git 冲突状态与文档中的编号连续性。
