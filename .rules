# Zflow 开发规范 (SPEC)

> 本文件是项目规范入口。任务执行仍遵循 `AGENTS.md` 的意图识别与文档驱动流程；本文件用于补齐工程实现规范。

## 1. 目标与技术基线

- 项目目标：构建开源、自托管 RSS 阅读器，逐步接入 LLM 评分、分类与探索推荐。
- 后端语言固定：Go。
- 首版输入源：RSS/Atom。
- 数据策略：默认零遥测（Zero Telemetry by Default）。

## 2. 仓库与模块边界

- `backend/`：Go 后端服务（API、抓取、持久化、调度）。
- `frontend/`：前端（当前为联调页，后续演进为正式 Web/PWA）。
- `.phrase/`：phase 文档、任务、变更、决策。
- `.claude/`：协作命令、提交规范、记忆索引。

约束：
- 前端只能通过 HTTP API 调用后端，禁止直接耦合抓取与存储实现。
- 业务逻辑优先放后端，前端负责交互与展示。

## 3. Go 后端规范

- 依赖注入：通过构造函数注入依赖，避免全局可变状态。
- 并发安全：共享状态必须使用 `sync.Mutex` / `sync/atomic` / channel。
- 错误处理：API 返回结构化错误，避免静默失败。
- 输入校验：所有外部输入（URL、JSON、查询参数）必须校验。
- 可维护性：函数保持小而直；超过 3 层嵌套必须重构。
- 测试要求：每个新增接口至少有一个成功路径测试和一个边界/失败路径测试。

## 4. API 与联调规范

- API 前缀：`/api/v1`。
- 健康检查：`/healthz`。
- CORS：后端必须支持浏览器预检请求（`OPTIONS`）和标准跨域头。
- 联调基线：
  - 后端默认 `http://localhost:8080`
  - 前端静态服务默认 `http://localhost:5173`

## 5. 文档驱动闭环

- 任何实现必须能追溯到 `taskNNN`。
- 完成实现后必须回写：
  - phase `task_*`（状态更新）
  - phase `change_*`（单行变更）
  - `.phrase/docs/CHANGE.md`（全局索引）
- 涉及不可逆技术决策必须新增/更新 ADR。

## 6. Git 与提交规范

- 提交标题采用 Conventional Commits（`feat/fix/docs/test/chore`）。
- 禁止使用“实现 taskXXX”作为提交标题，标题必须描述具体改动。
- 提交正文必须包含：
  - 背景/问题
  - 关键改动
  - 行为变化
- 提交正文必须使用真实换行，禁止字面 `\\n`。
- 改写历史后推送使用 `git push --force-with-lease`。

## 7. 分层记忆与上下文控制

- 记忆索引：`.claude/memory/MEMORY_INDEX.md`
- 分层加载：
  - 立项阶段：`memory_init.md`
  - 编码阶段：`memory_coding.md`
  - 文档阶段：`memory_docs.md`
  - 发布阶段：`memory_release.md`
- 规则：每次仅加载当前阶段所需记忆，避免全量加载造成 token 浪费。

