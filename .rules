# Zflow 开发规范 (Merged SPEC)

> 本文件融合 Zflow 与 Gist 约束。流程上仍遵循 `AGENTS.md`（意图识别 + 文档驱动）。

## 1. 目标与技术基线

- 目标：开源、自托管 RSS 阅读器，逐步接入 LLM 评分/分类/探索推荐。
- 后端：Go + Echo；SQLite 驱动必须使用 `modernc.org/sqlite`（无 CGO）。
- 前端：React + TypeScript + Vite。
- 首版输入：标准 RSS/Atom。
- 默认隐私：Zero Telemetry by Default。

## 2. 后端目录结构（强约束）

必须遵循以下结构并按职责放置代码：

```text
backend/
  cmd/server/              # 启动入口
  pkg/                     # 可复用基础设施（无业务状态）
    logger/
  internal/                # 业务私有实现
    handler/               # HTTP 协议适配层
    service/               # 业务编排层
    repository/            # 数据访问层
    model/                 # 领域模型
    db/                    # DB 初始化与迁移
    config/                # 配置加载
    scheduler/             # 定时任务
    feedparser/            # RSS/Atom 解析器
```

规则：
- `handler` 不直接写 SQL。
- `repository` 不承载业务规则。
- `service` 不处理 HTTP 协议细节。
- `pkg` 禁止依赖 `internal`。

## 3. 分层职责与依赖方向

- 依赖方向：`handler -> service -> repository -> db`
- `model` 作为共享领域对象，供 `service/repository` 使用。
- 所有依赖通过构造函数注入，禁止全局可变状态。
- 共享并发状态必须使用 `sync.Mutex` / `sync/atomic` / channel。

## 4. 测试与 Mock 规范

- 新增后端接口至少包含：
  - 1 条成功路径测试
  - 1 条失败或边界路径测试
- 优先黑盒测试（通过公开 API 验证行为）。
- Mock 规则：
  - 接口定义在 `service` 或 `repository` 层；
  - Mock 文件放在接口所在包的 `mock/` 子目录；
  - 不允许集中式 `testutil/mock_*` 大杂烩。
- 提交前最低要求：`go test ./...` 必须通过。

## 5. 数据库规范（不含具体表索引细节）

- SQLite 使用 WAL，启用 `foreign_keys` 与合理 `busy_timeout`。
- 修改 schema 时必须提供迁移路径，不允许破坏旧数据读取。
- 禁止字符串拼接 SQL（统一参数绑定）。
- 数据更新遵循：
  - 无效空值不覆盖有效旧值；
  - 批量操作优先单 SQL；
  - 层级关系变更要防循环引用。
- 表/索引细节在实际引入时补充进 phase 文档与本规范。

## 6. 网络解析与抓取规范

- Feed 解析使用宽容策略，兼容非标准 RSS/Atom。
- 必须支持 Conditional GET：
  - 请求：`If-None-Match` / `If-Modified-Since`
  - 响应：处理 `304 Not Modified`
- 请求失败需返回可诊断错误（至少包含 HTTP 状态或错误原因）。
- 默认 UA 清晰标识项目身份；避免伪装造成不可控兼容风险。

## 7. 安全规范

- 所有输入均视为不可信（Path/Query/Body/URL/脚本）。
- Handler 层必须做参数校验和 body 大小限制。
- 文件路径必须使用 `filepath.Clean` 与目录边界控制，防路径穿越。
- 禁止硬编码密钥；禁止日志输出 token/password/api key 原文。
- 前端 HTML 渲染必须先清洗（当前使用 DOMPurify）。

## 8. 环境变量规范

推荐统一前缀 `ZFLOW_`，并兼容通用变量回退：

- `PORT` / `ZFLOW_ADDR`
- `DATA_DIR` / `ZFLOW_DATA_DIR`
- `ZFLOW_DB_PATH`
- `ZFLOW_LOG_LEVEL`（`debug/info/warn/error`，默认 `info`）
- `ZFLOW_LOG_FORMAT`（`text/json`，默认 `text`）
- 兼容：`LOG_LEVEL`、`LOG_INFO_LEVEL`、`LOG_FORMAT`

## 9. 日志规范（前后端）

后端：
- 使用统一结构化日志（`pkg/logger`），禁止散落 `fmt.Print/log.Printf`。
- 标准字段：
  - `module`：`http/handler/service/repository/scheduler/network`
  - `action`：`create/fetch/update/delete/refresh/list/request`
  - `resource`：`feed/entry/folder/icon/settings/http`
  - `result`：`ok/failed/skipped/cancelled`
- 字段命名统一 `snake_case`。
- URL 日志只记录 host，不记录敏感 query/token。

前端：
- 统一通过 `src/lib/logger.ts` 输出日志，避免页面层散落 `console.*`。
- 默认仅在开发环境输出 `debug/info`；`warn/error` 可在生产保留。
- 日志只记录请求路径与状态，不打印敏感 payload。

## 10. 前后端接口一致性（强约束）

- 后端 DTO 变更后，必须同步更新前端 `types` 与 `api` 调用层。
- API 错误语义要稳定（错误码 + 错误文本）；前端统一拦截非 2xx。
- 时间字段统一格式（RFC3339/UTC）并在前端统一展示转换。
- 若 ID 超过 JS 安全整数，后端需返回字符串并同步前端类型。

## 11. 前端工程约束（融合 Gist）

### 11.1 架构与组件
- 目录分层建议：`src/components/`（UI/业务组件）、`src/lib/`（纯函数与渲染安全工具）、`src/hooks/`（状态编排 Hook）、`src/services/`（复杂异步流程）、`src/types/`（DTO 与领域类型）。
- `App.tsx` 只做页面编排；可复用逻辑必须下沉到 `lib/hooks/services`。
- 组件 Props 与 API 响应必须显式类型化，禁止 `any`。

### 11.2 性能工程
- 列表视图必须支持增量渲染（分页/无限滚动/虚拟化至少其一），禁止全量同步渲染长列表。
- 昂贵计算（筛选、排序、映射）需使用 `useMemo` 或纯函数缓存，避免每次 render 重算。
- 网络请求统一经过 API 层并可控缓存失效，避免页面散落重复请求。

### 11.3 安全与渲染（XSS）
- 富文本渲染必须先经过清洗，统一走安全渲染工具（当前基线为 DOMPurify）。
- 页面层禁止直接拼装不可信 HTML；`dangerouslySetInnerHTML` 只能消费已清洗内容。
- 外链默认 `rel="noreferrer"`，并限制不可信脚本注入路径。

### 11.4 深色模式
- 使用 CSS 变量定义主题语义色，最少支持 `light/dark(system)`。
- 默认跟随系统 `prefers-color-scheme`，不强制引入复杂主题系统。
- 新增样式优先使用语义变量，避免硬编码颜色散落。

### 11.5 资源与状态管理
- 异步副作用必须可清理（计时器、事件监听、长连接）。
- 全局状态仅保留必要共享数据；局部交互状态保持在组件内部。
- URL 或可恢复状态（筛选/排序）优先集中管理，避免跨组件隐式耦合。

### 11.6 PWA 与移动端交互
- PWA 能力按里程碑启用，未进入里程碑时只保留规范约束，不强制实现。
- 移动端触控元素建议尺寸 >= 40px，布局需兼容窄屏与安全区域。
- 固定定位控件不得遮挡主阅读区域关键操作。

### 11.7 测试规范（Vitest）
- 前端单元测试基线为 Vitest；测试文件与源码同目录，命名 `*.test.ts` / `*.test.tsx`。
- 纯函数（时间格式化、筛选排序、URL 解析、安全工具）应优先覆盖测试。
- 提交涉及前端逻辑变更时，至少通过 `npm run build`；已接入 Vitest 的模块应同步跑 `npm run test`。

## 12. 文档驱动闭环

- 所有实现必须可追溯到 `taskNNN`。
- 完成后必须回写：
  - phase `task_*`
  - phase `change_*`
  - `.phrase/docs/CHANGE.md`
- 不可逆决策必须更新 ADR。

## 13. Git 提交规范

- 使用 Conventional Commits（`feat/fix/docs/test/chore`）。
- 标题必须描述具体改动，禁止“实现 taskXXX”。
- 正文必须包含：背景、关键改动、行为变化（真实换行，禁止字面 `\n`）。
- 每次提交默认追加动态 Co-Author trailer：
  - `Co-Authored-By: <当前代理名> <noreply@assistant.local>`

## 14. 验收基线

- 前端改动至少执行：`npm run build`。
- 后端改动至少执行：`go test ./...`。
- 交互改动需提供最小手动验证路径。

## 5. 前端开发规范 (TypeScript/React)

### 5.1 架构与组件
- **目录结构**：
  - `src/api/`: API 调用封装（请求函数、类型定义、流式响应处理）。
  - `src/components/ui/`: 通用 UI 组件 (shadcn/ui)。
  - `src/components/{feature}/`: 业务组件 (如 `sidebar/`, `entry-list/`, `settings/`)。
  - `src/hooks/`: 自定义 React Hooks。
  - `src/lib/`: 工具函数（HTML 解析、日期处理、路由等）。
  - `src/services/`: 服务层（复杂异步逻辑，如批量翻译请求去重/取消）。
  - `src/stores/`: Zustand 状态管理。
  - `src/types/`: TypeScript 类型定义。
  - `src/i18n/`: 国际化配置。
- **逻辑分离**：UI 负责渲染，逻辑抽离至自定义 Hooks (`useFeeds`, `useEntries` 等)。
- **类型规范**：所有 Props 和 API 响应必须定义 Interface，**严禁**使用 `any`。
- **路径别名**：使用 `@` 别名指向 `src` 目录，import 使用 `@/components/...` 格式。
- **路由库**：使用 `wouter` 轻量路由，路由解析/构建逻辑集中在 `lib/router.ts`。

### 5.2 性能工程
- **状态管理**：使用 `TanStack Query` 处理 Feed 数据缓存与预加载：
  - 设置 `staleTime` 避免频繁重新请求 (如 30s)
  - 设置 `refetchInterval` 保持数据新鲜 (如 60s)
  - 使用 `invalidateQueries` 进行缓存失效
  - 分页使用 `useInfiniteQuery` + `getNextPageParam`
- **渲染优化**：列表页禁止同步执行复杂的 Readability 转换，使用虚拟滚动。
- **分页边界**：`hasMore` 判断应请求 `limit+1` 条数据，通过实际返回数量判断是否有下一页。
