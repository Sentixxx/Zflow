# Plan: convert quick-add button to borderless plus icon (task129)

## Goal
将内容导航里的“+订阅”按钮改成无边框纯加号样式。

## Scope
1. 新增并完成 task129。
2. 修改 ReaderPage.tsx：按钮文案改为单个“+”。
3. 修改 styles.css：去掉边框与背景，保留轻量 hover 反馈。
4. npm run build。
5. 回写 task/change/CHANGE。
