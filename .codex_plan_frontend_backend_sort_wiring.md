# Frontend Backend Sort Wiring Plan

1. Add a focused frontend test that proves article list requests include the selected `sort` mode.
2. Extend the API client and query hooks so article fetches can accept and forward `sort`.
3. Update reader data loading to use backend sorting as the primary source while keeping local read-filter handling intact.
4. Run targeted frontend tests and a production build, then update phase docs if this closes the unfinished wiring work.
