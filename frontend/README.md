# Frontend (React + Vite)

## Dev

```bash
cd frontend
npm install
npm run dev
```

默认地址：`http://localhost:5173`

## Build

```bash
npm run build
```

## API

页面内可配置 `API Base URL`，默认 `http://localhost:8080`。

