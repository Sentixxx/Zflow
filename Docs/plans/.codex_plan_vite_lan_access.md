# Vite LAN Access Plan

1. Update Vite dev server config to listen on all interfaces instead of localhost-only loopback.
2. Verify the current dev server process path so the correct app binary can be allowed through macOS Application Firewall.
3. Request elevated permission to add and unblock the Node binary for incoming connections.
4. Report the remaining step clearly if the current dev server must be restarted for the new host binding to take effect.
