# Article List Sort Dropdown Refresh Plan

1. Replace the native select-based sort control with a compact custom popover trigger and option list.
2. Keep the interaction minimal: show the current sort label, toggle the menu, close on outside click, and update the selected mode.
3. Restyle the control and menu so it reads as part of the toolbar instead of a browser-default form element.
4. Run a frontend build to verify the toolbar update compiles cleanly.
