# Scoring Design Doc Plan

1. Write the agreed first-phase scoring redesign into the current phase documents instead of leaving it in chat only.
2. Capture the architecture clearly: input gate, readability fallback, single-article features, recent-pool novelty, and LLM tag features.
3. Keep the scope explicit for phase 1 so later implementation does not drift into model training or user-facing taxonomy work.
4. Update the phase changelog and global CHANGE index so the design work is traceable.
