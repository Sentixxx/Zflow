# External Info Memory Rule Plan

1. Extend cross-stage memory rules so externally researched information used during development must be persisted for later paper writing.
2. Define the persistence minimum clearly as `summary + source URL`, avoiding vague archival requirements.
3. Update the shared memory index and all stage-specific memory files so the rule applies consistently across init/coding/docs/release work.
4. Append change records to the current phase changelog and the global CHANGE index under the existing memory-rule task chain.
