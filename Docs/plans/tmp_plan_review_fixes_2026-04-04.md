# 临时计划：根据 backend review 修复高优先级问题

1. 先写或更新后端测试，覆盖以下行为：
   - AI 设置 GET 不返回明文 API key，只返回掩码与是否已配置状态。
   - CORS 默认仅允许本地开发来源，并支持按环境变量覆盖。
   - 文章列表查询继续返回正确分页/排序/作用域结果，但不再依赖全表加载。
2. 实现安全修复：
   - 掩码 AI API key 响应。
   - 将 CORS 放到可配置 allowlist，而不是固定 `*`。
   - 为 `ensureColumn` 增加“仅硬编码标识符”注释。
3. 实现性能/代码质量修复：
   - 将文章列表过滤、排序、分页下推到 SQLite 查询，列表查询排除重字段。
   - 将 folder 过滤的 `ListFolders()` 查库移出循环。
   - 预编译摘要提取相关正则，删除自定义 `min`。
4. 跑后端整包测试与前端构建，确认无回归。
5. 回写 phase task/change/CHANGE，形成清晰提交边界后自动 commit + push。
