# API Base LAN Fix Plan

1. Add a focused pure-function test for resolving the frontend default API base from the current browser host.
2. Implement a small helper that maps `localhost/127.0.0.1` to local development and uses the current hostname for LAN access.
3. Replace the hard-coded `http://localhost:8080` defaults in reader/settings pages with the helper while preserving `localStorage` override behavior.
4. Run targeted tests and a frontend build to verify the regression is fixed without broad UI changes.
