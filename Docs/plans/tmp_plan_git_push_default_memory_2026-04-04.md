# Temp Plan: Git Push Default Memory (2026-04-04)

## Goal
- 将长期推送偏好收敛为：统一优先使用真实 `git push`。

## Scope
- 仅更新自动提交相关 memory。
- 不改其它流程文档。

## Steps
1. 将 `memory_autocommit.md` 中的远端提交优先级改为真实 `git push` 优先。
2. 删除“默认 MCP 优先”和“本次例外”的表述，避免后续歧义。
